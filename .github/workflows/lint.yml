name: lint
on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - name: ansible-lint
        run: | 
          ansible-playbook  ${{ github.workspace }}/sysctl/sysctl_vars.yaml --check

