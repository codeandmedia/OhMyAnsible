name: lint
on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: ansible-lint
        uses: ansible/ansible-lint-action@master
        with: |
          sysctl/sysctl_vars.yaml
